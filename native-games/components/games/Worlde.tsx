import { StyleSheet, Text, View } from "react-native";
import React from "react";
import ThemedView from "../ThemedView";

const Worlde = () => {
	return (
		<ThemedView>
			<Text>Worlde game</Text>
		</ThemedView>
	);
};

export default Worlde;

const styles = StyleSheet.create({});
